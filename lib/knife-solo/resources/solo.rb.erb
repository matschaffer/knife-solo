base = File.expand_path('..', __FILE__)

role_path                 base + '/roles'
data_bag_path             base + '/data_bags'
encrypted_data_bag_secret base + '/data_bag_key'

cookbook_path             [ <%= cookbook_paths.map { |p| "base + '/' + " + File.basename(p).inspect }.join(', ') %> ]

